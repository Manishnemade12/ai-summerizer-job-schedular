# Server
server.port=${PORT:8080}

# Redis / Valkey
spring.data.redis.url=${REDIS_URL:redis://localhost:6379}

# Gemini AI
gemini.api-key=${GEMINI_API_KEY:}

# Worker Pool
worker.count=${WORKER_COUNT:3}

# Cache TTL in seconds
cache.ttl=${CACHE_TTL:300}
